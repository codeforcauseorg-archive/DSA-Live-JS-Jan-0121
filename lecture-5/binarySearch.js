let arr = [2, 10, 3, 16, 4]
arr.sort(sortNum)
// console.log(arr)

function sortNum(a, b) { // a = 10, b= 20 then a-b = -10
    return a - b; //-1 ascending order else descending order
}
let array = new Array((2 ** 32) - 1)
console.log(Number.MAX_SAFE_INTEGER)
// console.log(array.length)